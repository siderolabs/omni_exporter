global:
  # short interval so the data moves quickly while iterating
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "omni_exporter"
    static_configs:
      - targets: ["host.docker.internal:10048"]
