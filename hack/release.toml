# commit to be tagged for the new release
commit = "HEAD"

project_name = "omni_exporter"
github_repo = "siderolabs/omni_exporter"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
